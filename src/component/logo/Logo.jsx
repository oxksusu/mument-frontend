import styles from './logo.module.css';

const Logo = () => {

    return(
        <div>
            <a href="/">
                <h1 className={styles.logoStyle}>mument.</h1>
            </a>
        </div>
    )
}

export default Logo;